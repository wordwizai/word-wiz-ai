# API URL (goes through nginx)
VITE_API_URL=https://api.wordwizai.com

# WebSocket URL (direct connection to bypass nginx buffering)
# This connects directly to port 8000 to avoid the 20-second delay
VITE_WS_URL=ws://api.wordwizai.com:8000
