# GPT Prompt Test Cases

Each test case in this folder should include:
- `input.json`: JSON with a fully analyzed pronunciation data object (see `AudioAnalysis` schema)

Example structure:
```
test_case_01.json
```
